# Development

## Backend

Launch our backend

```bash
cd back
go run main.go --host <docker-registry-url>
```

## Frontend

In another terminal session, let's run our front app

```bash
cd front
yarn dev
```
